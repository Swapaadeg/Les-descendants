# Configuration d'environnement - Template
# Copiez ce fichier en .env et remplacez les valeurs par vos credentials réels

# JWT Secret Key (générez une clé aléatoire sécurisée)
# Vous pouvez générer une clé avec: openssl rand -hex 64
JWT_SECRET=your-secret-key-here-min-32-characters

# Database
DB_HOST=localhost
DB_NAME=arki-family
DB_USER=root
DB_PASS=your-database-password

# URLs
BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

# Email SMTP Configuration
# Pour Gmail:
#   1. Activez l'authentification à 2 facteurs
#   2. Générez un mot de passe d'application: https://myaccount.google.com/apppasswords
# Pour Mailtrap (développement):
#   Créez un compte sur https://mailtrap.io et copiez les credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Arki'Family

# Email Mode: 'debug' (logs only) or 'smtp' (real emails)
EMAIL_MODE=smtp

# Debug Mode
DEBUG_MODE=true
